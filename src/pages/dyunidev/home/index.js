import { Box } from '@mui/material';
import React from 'react';

const Home = (props) => {

  return <div>
       <Box sx={{ bgcolor: '#cfe8fc', height: '100vh' }} />
  </div>;
};

export default Home;
